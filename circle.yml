machine:
  python:
    version: 2.7.11

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install nginx

deployment:
  production:
    branch: master
    commands:
      - ./sample.py

